#include <stdio.h>
#include <stdlib.h>
#include "include/defs.h"
#include "include/cur.h"

double queryRate(Cur currency) {
	double rate; 

	return rate;
}

Hist *queryHistory(Cur currency, int days) {
	
	Hist *history;

	return history;
}
